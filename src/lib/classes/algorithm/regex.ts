export const regularPolygonRegex = /^\d+$/;
export const regularStarRegex = /\{(\d+)\.(\d+)(o|i)?\}/;
export const parametricStarRegex = /\{(\d+)\|(\d+)(o|i)?\}/;
export const equilateralPolygonRegex = /^(\d+)\((\d+(;\d+)*)\)$/;